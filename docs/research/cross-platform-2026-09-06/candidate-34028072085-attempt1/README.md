# Blocked first native matrix

Seven targets passed. macOS Intel failed the .NET 8 fixture at configurationDone before its upstream suite could start. The same target passed all 30 upstream .NET 10 scenarios. Aggregation failed and packaging/publication were skipped. These are failed-attempt records, retained separately from any rerun. Runner paths are normalized; provenance records both original and archived hashes.
